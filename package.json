{
  "name": "screen-share-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k --success first -n server,vite,electron -c blue,cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\" \"npm --prefix client run electron:dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run start",
    "kill": "kill-port 3001 5173"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
